**CRITICAL** never ever read a .env file or perform any command that outputs its content in the chat history (such as `cat`).

**Skills are specifications, not guidelines.** After loading a skill, validate your work against every requirement before proceeding.

**Commit proactively** if and only if an `auto-commit-and-push` or `auto-commit-dont-push` skill is loaded. Otherwise, wait for explicit instruction.
