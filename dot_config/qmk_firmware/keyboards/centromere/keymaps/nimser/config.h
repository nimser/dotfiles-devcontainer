#define ONESHOT_TAP_TOGGLE 3  /* Tapping this number of times holds the key until tapped once again. */
#define ONESHOT_TIMEOUT 1000  // timeout during which the oneshot action can be performed

#define TAPPING_TOGGLE 4 // for the TT feature. Number of taps to go in and out of the mode
#define TAPPING_FORCE_HOLD_PER_KEY // makes it possible to use a dual role key as modifier shortly after having been tapped
