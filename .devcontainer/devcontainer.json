{
  "build": {
    "context": ".",
    "dockerfile": "Dockerfile"
  },
  "features": {
    "ghcr.io/devcontainers/features/nix:1": {
      "multiUser": true
    }
  },
  "remoteEnv": {
    "DISPLAY": "${localEnv:DISPLAY}",
    "GITHUB_TOKEN": "${localEnv:GITHUB_PAT_PUBLIC_REPO}",
    "GIT_COMMITTER_EMAIL": "${localEnv:GIT_COMMITTER_EMAIL}",
    "GIT_COMMITTER_NAME": "${localEnv:GIT_COMMITTER_NAME}",
    "GIT_AUTHOR_NAME": "${localEnv:GIT_AUTHOR_NAME}",
    "GIT_AUTHOR_EMAIL": "${localEnv:GIT_AUTHOR_EMAIL}",
    "GOOGLE_API_KEY": "${localEnv:GOOGLE_API_KEY}",
    "GEMINI_API_KEY": "${localEnv:GOOGLE_API_KEY}"
  },
  "containerEnv": {
    "CHEZMOI_SOURCE_DIR": "/home/vscode/.local/share/chezmoi"
  },
  "workspaceMount": "source=${localWorkspaceFolder},target=/home/vscode/.local/share/chezmoi,type=bind",
  "workspaceFolder": "/home/vscode/.local/share/chezmoi",
  "mounts": [
    // Persistent caches. A *named* volume (source=<name>,type=volume) is a
    // Docker object with its own lifecycle: containers only attach to it, so
    // `--recreate` (docker rm + run) and the weekly cleanup leave it intact
    // (`docker volume prune` only removes *anonymous* volumes). On first mount
    // over a non-empty image path (/nix), Docker seeds the empty volume from
    // the image; afterwards the volume always wins over image content. Net
    // effect: nix-env / mise install are near no-ops on rebuild. To re-seed
    // after a nix feature upgrade: `docker volume rm devpod-nix`.
    "source=devpod-nix,target=/nix,type=volume",
    "source=devpod-mise,target=/home/vscode/.local/share/mise,type=volume",
    "source=devpod-cache,target=/home/vscode/.cache,type=volume",
    "source=/tmp/.X11-unix,target=/tmp/.X11-unix,type=bind",
    "source=${localEnv:HOME}/.ssh/git-signing.pub,target=/mnt/host/git-signing.pub,type=bind,readonly",
    "source=${localEnv:HOME}/.ssh/gopass-deploy-key,target=/mnt/host/gopass-deploy-key,type=bind,readonly",
    "source=${localEnv:HOME}/skills,target=/home/vscode/skills,type=bind",
    "source=${localEnv:HOME}/.pi,target=/home/vscode/.pi,type=bind",
    "source=${localEnv:HOME}/.claude,target=/home/vscode/.claude,type=bind"
  ],
  "runArgs": [
    "--net=host",
    "--ipc=host",
    "--cap-add=SYS_ADMIN",
    "--security-opt=seccomp=unconfined"
  ],
  "customizations": {
    "vscode": {
      "extensions": [
        "saoudrizwan.claude-dev",
        "esbenp.prettier-vscode",
        "astro-build.astro-vscode"
      ]
    },
    "settings": {
      "editor.defaultFormatter": "esbenp.prettier-vscode",
      "editor.formatOnSave": true,
      "prettier.semi": false,
      "[astro]": {
        "editor.defaultFormatter": "astro-build.astro-vscode"
      },
      "[typescript]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode"
      },
      "[json]": {
        "editor.defaultFormatter": "esbenp.prettier-vscode"
      }
    }
  },
  "onCreateCommand": "sudo chown -R vscode:vscode /home/vscode/.local /home/vscode/.cache && mkdir -p /home/vscode/.ssh && sudo chown vscode:vscode /home/vscode/.ssh && mkdir -p /home/vscode/.local/state/age-plugin-tpm /home/vscode/.local/state/age-plugin-yubikey && sudo chown -R vscode:vscode /home/vscode/.pi && sudo chown -R vscode:vscode /home/vscode/.claude",
  "postCreateCommand": ".devcontainer/scripts/setup && bash /home/vscode/.local/share/chezmoi/setup"
}
